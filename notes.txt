https://github.com/cmu-db/benchbase usa postgres, ver se compensa.
deve ser em thread e não async porque o benchmark reinicia várias vezes,
assim como a coleta das métricas, agora que eu movi os outros para a fase de preparo está ok


verificar: o caso de fazer o backup e fazer o drop é para n influenciar as métricas do mysql? talvez, hein
mas mesmo assim, pode ser feito menos coisa, eu acho.

[x] rodar o projeto
[x] fazer um jeito menos zoado de iniciar os containers que n seja por cmd (se der, n é prioridade)
[ ] sincronizar as coisas, tipo se um terminou, cabô todos e tal



- preciso checar as pastas para rodar o benchmark
- investigar o rolê do backup, parece que tem que ser feito uma vez, e depois a cada vez que for rodar o benchmark o restore e tal.
do jeito que está, não tem como rodar mais de uma vez, pra rodar mais de uma vez precisaria:
- ter uma automatização de ver a quantidade de arquivos de configs de db, e benchmark